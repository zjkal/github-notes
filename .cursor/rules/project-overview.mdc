---
description: GitHub Notes 项目架构与核心约束
alwaysApply: true
---

# 项目总览

GitHub Notes 是一个 Manifest V3 Chromium 扩展(Chrome/Edge),为 GitHub 仓库添加本地备注。

## 核心约束(不可违反)

- **原生 JavaScript(ES6+)**:禁止引入 TypeScript、React/Vue、打包器(webpack/vite)、npm 依赖,除非用户明确要求
- **无构建流程**:直接修改源文件,浏览器"重新加载扩展"即可生效;发布打包用 `.\build.ps1`
- **纯本地存储**:所有数据存 `chrome.storage.local`,禁止添加远程 API 调用或数据上报
- **权限最小化**:manifest 权限仅 `storage`、`activeTab`、`sidePanel` 和 `https://github.com/*`,新增权限需谨慎

## 目录职责

| 路径 | 职责 |
|---|---|
| `src/content.js` | 注入 GitHub 页面:仓库备注 + Stars 列表备注 |
| `src/background.js` | Service Worker:设置、导入导出、消息路由 |
| `src/popup.js` | 弹窗/侧边栏 UI(共用 `pages/popup.html`) |
| `src/options.js` | 选项页逻辑 |
| `src/i18n.js` | 页面 i18n 工具(`data-i18n`、全局 `t()`) |
| `pages/` | HTML 页面,样式写在页内 `<style>` |
| `styles/content.css` | 仅 content script 注入的样式 |
| `_locales/en`、`_locales/zh_CN` | 双语文案,必须同步维护 |

## 存储键约定

- 备注键:`owner/repo`(如 `torvalds/linux`),值含 `content`、`createdAt`、`updatedAt`、`version`
- 插件内部键:统一 `plugin_` 前缀(如 `plugin_settings`、`plugin_metadata`)
- 判断是否为备注:`!key.startsWith('plugin_') && value.content !== undefined`
- 勿破坏导入导出的 JSON 数据形状

## 规范来源

编码规范以 `CONTRIBUTING.md` 为权威,修改行为约定时需同步更新该文件。
